# macOS
.DS_Store

# Windows
Thumbs.db

# Claude Code per-user settings（個人權限快取，不可共用）
.claude/settings.local.json

# Notion publish mapping（v1 X-phase：暫時指向 personal workspace，不入 git）
# team workspace 授權搞定後，移除此行並 `git add -f .claude/notion-mapping.json`
.claude/notion-mapping.json
